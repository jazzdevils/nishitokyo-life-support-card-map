## Agent skills

### Issue tracker

Issues are stored as markdown files in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Using default triage labels: needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout with CONTEXT.md at repo root. See `docs/agents/domain.md`.
